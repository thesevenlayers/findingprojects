{% for sponser in sponsers %}
    <div class="col-md-3 col-sm-4 mix">
        <div class="mix-inner">
            <div class="imgList">
                <img class="img-responsive" src="{{ sponser.getWebPath ? asset(sponser.getWebPath) : "http://www.placehold.it/200x150/EFEFEF/AAAAAA&text=no+image" }}" alt="">
            </div>
            <div class="mix-details">
                <div class="col-md-12 iconGroup">
                    <a href="{{ path("fp.be.sponser.get_edit", {"id": sponser.id}) }}" class="mix-link pull-left btnRight">
                        <i class="fa fa-edit"></i></a>
                    {#<form action="{{ path("fp.be.home.delete", {"id": sponser.id }) }}" method="post">#}
                    {{ form_start(delete_form, {"action": path("fp.be.sponser.delete", {"id": sponser.id }), "attr": {"class": "pull-left btnRight"}, "method":"POST" } ) }}
                    <a href="#{{ sponser.id }}" class="mix-link" data-toggle="modal">
                        <i class="fa fa-trash-o"></i>
                    </a>
                    <div class="modal fade" id="{{ sponser.id }}" tabindex="-1" role="basic" aria-hidden="true">
                        <div class="modal-dialog">
                            <div class="modal-content">
                                <div class="modal-header">
                                    <button type="button" class="close" data-dismiss="modal" aria-hidden="true"></button>
                                </div>
                                <div class="modal-body">
                                    Are you sure you want to delete this?
                                </div>
                                <div class="modal-footer">
                                    <button type="button" class="btn default" data-dismiss="modal">Close</button>
                                    <button type="submit" class="btn red"><i class="fa fa-trash-o"></i> Delete</button>
                                </div>
                            </div>
                            <!-- /.modal-content -->
                        </div>
                        <!-- /.modal-dialog -->
                    </div>
                    {{ form_rest(delete_form) }}
                    {{ form_end(delete_form) }}
                    {% if sponser.getWebPath %}
                        <a class="mix-preview pull-left btnRight fancybox-button" href="{{ asset(sponser.getWebPath) }}" title="Project Name" data-rel="fancybox-button"><i class="fa fa-search"></i></a>
                    {% endif %}
                </div><!--End ICon Group-->
            </div>
        </div>
    </div>
{% endfor %}